# Development and test dependencies for services/api.
#
# Installed on top of requirements.txt:
#     pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

# --- tests -------------------------------------------------------------------
pytest==8.3.5
# Async test functions, for the request-path tests that use the async engine.
pytest-asyncio==0.26.0
pytest-cov==6.1.1
# Freezes time so token expiry can be tested without sleeping.
time-machine==2.16.0

# --- lint and types ----------------------------------------------------------
# Ruff covers formatting and linting; alembic.ini runs it as a post-write hook
# so generated migrations match the rest of the codebase.
ruff==0.11.10
mypy==1.15.0
# Stubs mypy needs to check the SQLAlchemy-adjacent code accurately.
types-python-dateutil==2.9.0.20241206
